# Example environment variables for local development
# Copy this to `.env` and set real values. Do NOT commit secrets.

MONGODB_URI=mongodb://localhost:27017/usuario-app

# Add other variables used by your app below (without secrets)
# PORT=3000